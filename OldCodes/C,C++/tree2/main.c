#include <stdio.h>
#include <stdlib.h>

int main()
{
   typedef struct tree
    {
        int info;
        struct tree *left;
        struct tree *right;
    }T;
     T *root=NULL,*t,*subr,*subl,*temp;

int main()
{
    int level=1,i=1;
    char ch;
    t=(T *)malloc(sizeof(T));
    return 0;
}

void tree()
    {
    while(level=i)
    {
        temp=t;
        printf("Enter Info\n");
        scanf("%d",&t->info);
        if(subl!=NULL)
        {
            subl=temp;
            tree();
        }
    }
}
